import { addUntil100 } from "../addUntil100";

// gw ga tau gimana ngetesnya
describe("Add until 100", () => {
  it("should ", () => {
    const result = addUntil100([5, 85, 15, 10, 5]);
    console.log(result);
  });
});
