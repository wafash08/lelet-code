import { uniquePaths } from "../uniquePaths";

describe("Find shortest unique paths", () => {
  it("should find the shortest paths", () => {
    const result = uniquePaths(3, 7);
    console.log(result);
  });
});
